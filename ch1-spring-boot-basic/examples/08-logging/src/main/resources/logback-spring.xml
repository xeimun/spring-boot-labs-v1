<?xml version="1.0" encoding="UTF-8"?>
<configuration>
    <!-- 스프링 부트의 기본 로그 설정 가져오기 -->
    <include resource="org/springframework/boot/logging/logback/defaults.xml"/>
    
    <!-- 콘솔 출력 설정 -->
    <appender name="CONSOLE" class="ch.qos.logback.core.ConsoleAppender">
        <encoder>
            <pattern>%clr(%d{HH:mm:ss.SSS}){faint} %clr(${LOG_LEVEL_PATTERN:-%5p}) %clr(---){faint} %clr([%15.15t]){faint} %clr(%-40.40logger{39}){cyan} %clr(:){faint} %m%n${LOG_EXCEPTION_CONVERSION_WORD:-%wEx}</pattern>
            <charset>UTF-8</charset>
        </encoder>
    </appender>
    
    <!-- 파일 출력 설정 -->
    <appender name="FILE" class="ch.qos.logback.core.rolling.RollingFileAppender">
        <file>logs/myapp.log</file>
        <rollingPolicy class="ch.qos.logback.core.rolling.TimeBasedRollingPolicy">
            <fileNamePattern>logs/myapp-%d{yyyy-MM-dd}.log</fileNamePattern>
            <maxHistory>7</maxHistory> <!-- 7일간 로그 보관 -->
        </rollingPolicy>
        <encoder>
            <pattern>%d{yyyy-MM-dd HH:mm:ss.SSS} [%thread] %-5level %logger{36} - %msg%n</pattern>
            <charset>UTF-8</charset>
        </encoder>
    </appender>
    
    <!-- 개발 환경 설정 -->
    <springProfile name="dev">
        <!-- 애플리케이션 패키지는 DEBUG 레벨로 로깅 -->
        <logger name="com.example.logging" level="DEBUG" />
        
        <!-- 스프링 웹 로그도 DEBUG 레벨로 출력 -->
        <logger name="org.springframework.web" level="DEBUG" />
        
        <root level="INFO">
            <appender-ref ref="CONSOLE" />
            <appender-ref ref="FILE" />
        </root>
    </springProfile>
    
    <!-- 운영 환경 설정 -->
    <springProfile name="prod">
        <!-- 애플리케이션 패키지는 INFO 레벨로 로깅 -->
        <logger name="com.example.logging" level="INFO" />
        
        <root level="WARN">
            <appender-ref ref="CONSOLE" />
            <appender-ref ref="FILE" />
        </root>
    </springProfile>
    
    <!-- 기본 설정 (프로파일이 지정되지 않은 경우) -->
    <springProfile name="default">
        <logger name="com.example.logging" level="DEBUG" />
        
        <root level="INFO">
            <appender-ref ref="CONSOLE" />
        </root>
    </springProfile>
</configuration> 